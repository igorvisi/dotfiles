#!/usr/bin/env bash
set -euo pipefail

config="${XDG_CONFIG_HOME:-$HOME/.config}/hyprland-quattro/hyprland.conf"

if [[ ! -r "$config" ]]; then
    printf 'Hyprland Quattro config not found: %s\n' "$config" >&2
    exit 1
fi

exec uwsm start -F -e -D Hyprland -- Hyprland --config "$config"
