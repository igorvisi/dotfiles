- defaults:
    link:
      create: true
      force: true
      relative: true
      relink: true

- link:

    # SSH
    ~/.ssh/conf: apps/ssh/linux.config


- shell:
    - [
        "sudo apt install zsh bat eza dnsutils git neovim curl btop"
      ]
