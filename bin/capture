#!/bin/bash

# get screen capture with same name format with my Android phone screen capture and move it to $SCREENSHOTDIR
#
source .global
scrot "$SCREENSHOTDIR/Screenshot_%Y-%m-%d-%H-%M-%S.png" &&
notify-send --icon=applets-screenshooter "I got a screenshot !"
