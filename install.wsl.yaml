- defaults:
    link:
      create: true
      force: true
      relative: true
      relink: true

# WSL remains CLI-only. Common links come from install.yaml, while Windows
# application settings remain on Windows.
